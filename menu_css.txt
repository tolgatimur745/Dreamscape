
    /* ═══ HAMBURGER MENU ═══ */
    .menu-ham{display:flex;flex-direction:column;gap:5px;background:none;border:none;cursor:pointer;padding:8px 10px;border-radius:10px;transition:var(--tb);flex-shrink:0}
    .menu-ham span{display:block;width:22px;height:2px;background:var(--tx);border-radius:2px;transition:var(--tb)}
    .menu-ham:hover span{background:var(--a3)}
    .menu-ham.open span:nth-child(1){transform:translateY(7px) rotate(45deg)}
    .menu-ham.open span:nth-child(2){opacity:0;transform:scaleX(0)}
    .menu-ham.open span:nth-child(3){transform:translateY(-7px) rotate(-45deg)}

    /* ═══ MENU OVERLAY ═══ */
    .menu-overlay{position:fixed;inset:0;background:rgba(4,2,14,.97);z-index:9999;opacity:0;pointer-events:none;transition:opacity .35s ease;overflow-y:auto;backdrop-filter:blur(30px)}
    .menu-overlay.open{opacity:1;pointer-events:all}
    .menu-overlay-top{position:sticky;top:0;z-index:10;display:flex;align-items:center;justify-content:space-between;padding:1.2rem 2rem;background:rgba(4,2,14,.9);backdrop-filter:blur(20px);border-bottom:1px solid rgba(255,255,255,.06)}
    .menu-back-btn{display:flex;align-items:center;gap:.5rem;background:var(--glass);border:1px solid var(--gb);color:var(--tx2);padding:8px 18px;border-radius:100px;cursor:pointer;font-family:'Outfit',sans-serif;font-size:.88rem;font-weight:600;transition:var(--tb)}
    .menu-back-btn:hover{background:var(--gh);color:var(--tx);transform:translateX(-3px)}
    .menu-overlay-logo{font-size:1.1rem;font-weight:900;background:var(--grad);-webkit-background-clip:text;-webkit-text-fill-color:transparent}
    .menu-search-wrap{position:relative}
    .menu-search{padding:8px 16px 8px 36px;border-radius:100px;border:1px solid var(--gb);background:var(--glass);color:var(--tx);font-family:'Outfit',sans-serif;font-size:.85rem;outline:none;transition:var(--t);width:180px}
    .menu-search:focus{border-color:var(--a1);width:220px}
    .menu-search-icon{position:absolute;left:12px;top:50%;transform:translateY(-50%);font-size:.85rem;pointer-events:none}
    .menu-overlay-hero{text-align:center;padding:3rem 2rem 1.5rem}
    .menu-overlay-hero h2{font-size:2.5rem;font-weight:900;background:var(--grad);-webkit-background-clip:text;-webkit-text-fill-color:transparent;margin-bottom:.4rem}
    .menu-overlay-hero p{font-size:.95rem;color:var(--tx3)}
    .menu-content{padding:0 2rem 4rem;max-width:1100px;margin:0 auto}
    .menu-cat-label{font-size:.7rem;font-weight:700;text-transform:uppercase;letter-spacing:2.5px;color:var(--tx3);margin:2rem 0 .8rem;padding-left:.2rem;display:flex;align-items:center;gap:.5rem}
    .menu-cat-label::after{content:'';flex:1;height:1px;background:var(--gb)}
    .menu-cards-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(148px,1fr));gap:.8rem}
    .menu-card{padding:1.3rem .9rem;background:var(--glass);border:1px solid var(--gb);border-radius:var(--r3);cursor:pointer;transition:var(--tb);display:flex;flex-direction:column;align-items:center;gap:.4rem;text-align:center;position:relative;overflow:hidden}
    .menu-card::before{content:'';position:absolute;inset:0;background:var(--grad);opacity:0;transition:var(--t)}
    .menu-card:hover{transform:translateY(-5px);box-shadow:0 16px 40px rgba(0,0,0,.5);border-color:rgba(255,255,255,.15)}
    .menu-card:hover::before{opacity:.06}
    .menu-card-em{font-size:2rem;line-height:1}
    .menu-card-ttl{font-size:.82rem;font-weight:700;color:var(--tx)}
    .menu-card-dsc{font-size:.68rem;color:var(--tx3);line-height:1.4}
    .menu-card.hidden{display:none}

    /* ═══ SIMPLIFIED NAVBAR ═══ */
    .navbar{padding:.8rem 1.4rem}
    .nav-links{display:none}
